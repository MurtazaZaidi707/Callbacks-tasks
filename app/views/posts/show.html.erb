<h1><%= @post.title %></h1>

<p><%= @post.body %></p>

<p><%= @post.age %></p>

<p><%= @post.email %></p>

<ul>
  <li><%= link_to "Destroy", post_path(@post), data: {
                    turbo_method: :delete,
                    turbo_confirm: "Are you sure?"
                  } %></li>
</ul>

<%= link_to "Back to posts", posts_path %>
